{
  "name": "auth",
  "version": "0.0.0",
  "private": true,
  "main": "dist/index.js",
  "scripts": {
    "compile": "tsc",
    "precompile": "rm -rf dist && tslint -c tslint.json -p tsconfig.json --fix",
    "start": "./node_modules/.bin/tsc-watch --onSuccess \"node .\" --noClear"
  },
  "dependencies": {
    "@types/morgan": "^1.9.0",
    "bcrypt": "^4.0.1",
    "dotenv": "^8.2.0",
    "express": "^4.17.3",
    "morgan": "^1.9.1",
    "reflect-metadata": "^0.1.13"
  },
  "devDependencies": {
    "@types/express": "^4.17.2",
    "@types/node": "^13.7.7",
    "tsc-watch": "^4.2.3",
    "tslint": "^6.0.0",
    "typescript": "^3.8.3"
  }
}
